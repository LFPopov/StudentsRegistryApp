pipeline {
    agent any

    stages{
        stage('Checkout'){
            steps{
                git branch: 'main', url: 'https://github.com/LFPopov/StudentsRegistryApp'     
            }
        }

        stage('Install dependencies'){
            steps{
                scrypt{
                    bat 'npm install'
                }
            }
        }

        stage('Start application and run tests'){
            steps{
                scrypt{
                    bat 'npm start &'
                    bat 'wait-on https://localhost:8082'
                    bat 'npm test'
                }
            }
        }
    }

    post{
        always{
            echo 'CI pipeline completed'
        }
    }
}